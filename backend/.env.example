# Set your OpenAI API key (preferred provider) or leave empty to use the mock/local fallback
OPENAI_API_KEY=
# Set your Gemini (Google) API key if you prefer Gemini. Leave empty if unused.
GEMINI_API_KEY=AIzaSyBGwCjYeKRfwjuntf0Hs1nRI9j2EXTMz7A
# DEFAULT_LLM_PROVIDER: one of 'openai', 'gemini', or 'mock' (mock uses local deterministic fallback)
DEFAULT_LLM_PROVIDER=gemini
DEFAULT_LLM_MODEL=gemini-3.5-flash
EMBEDDING_MODEL=BAAI/bge-small-v1.5
CORS_ORIGINS=http://localhost:3000
CHROMA_PERSIST_DIRECTORY=data/chroma
CHROMA_COLLECTION_NAME=research_documents
DATA_DIRECTORY=data
REPORTS_DIRECTORY=reports
